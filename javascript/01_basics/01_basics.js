const acountId=14453; //can not be changes
let accountBalance=1000.50;
var accountType="savings";
accountcity="New York";

accountId=2;
console.log(accountId);

/*prefer not to use var, use let or const instead
const is used for constants, let is used for variables that can change
because of function scope, let is preferred over var and global scope*/

